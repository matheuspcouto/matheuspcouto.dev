/**
 * Constantes de timing utilizadas na aplicação.
 *
 * @author Matheus Pimentel Do Couto
 */

/** Delay em ms para esconder o loader após inicialização. */
export const LOADER_DELAY_MS = 2000;

/** Velocidade de digitação em milissegundos. */
export const TYPE_SPEED_MS = 100;

/** Velocidade de deleção em milissegundos. */
export const DELETE_SPEED_MS = 50;

/** Tempo de pausa ao terminar de digitar uma palavra em ms. */
export const TYPING_PAUSE_MS = 2000;

/** Delay da animação do Bootstrap Offcanvas em ms. */
export const OFFCANVAS_ANIMATION_DELAY_MS = 350;

/** Duração da animação de scroll suave em ms. */
export const SMOOTH_SCROLL_DURATION_MS = 1000;

/** Altura da navbar para cálculo de offset em px. */
export const NAVBAR_HEIGHT_PX = 90;

/** Limite de scroll para considerar "no topo" em px. */
export const SCROLL_TOP_THRESHOLD_PX = 100;

/** Breakpoint para transição mobile/desktop em px. */
export const DESKTOP_BREAKPOINT_PX = 991;
